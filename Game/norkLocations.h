#ifndef NORKLOCATIONS_H
#define NORKLOCATIONS_H
using namespace std;

class NorkLocations
{
public:
    void townOfNork(string name, int& yen, bool kingStatus, bool& hasPlayedDeathDarts);
    void library(string name, int& yen, bool kingStatus, bool& hasPlayedDeathDarts);
};

#endif